# Veenero Frontend Environment Variables
# Copy this file to .env.local for local overrides (never commit real secrets)

# Backend API base URL — no trailing slash
VITE_API_URL=https://veenero-website.onrender.com
